\- **Port Kaisola from Electron to Swift-native.** Goals: native feel + kill Electron memory (measured ~880 MB/window today), and share one Swift core across Mac + iPhone. [Full design spec](superpowers/specs/2026-07-20-kaisola-swift-native-design.md) (rev3, two rounds of repo-grounded plan-review) · [Phase 0/1 design](superpowers/specs/2026-07-21-kaisola-swift-phase-0-1-design.md) · [implementation plan](superpowers/plans/2026-07-21-kaisola-swift-phase-0-1.md). — **strategy and Phase 0/1 approved; Phase 0 is integrated and the Phase 1 observe-only native preview has crossed the local implementation boundary as of 2026-07-21. It now has the complete read inventory, private cursor checkpoints, bounded reconnect/wake recovery, exact suffix resume, a checksum-pinned universal Node/node-pty helper, sealed per-user bootstrap, independent N/N+1 versions, Sparkle preview-channel integration, synthetic N → N+1 → rollback continuity over one real PTY, a same-metric physical-footprint harness, and automated SwiftTerm framing/scrollback/ANSI/Unicode/resize stress gates. An exact sealed universal `LocalRelease` artifact also passed its native launch probe and a direct AppKit/accessibility-tree shell inspection, including the correct read-only/offline coexistence response to an older live Electron broker and sidebar hide/show. Paired captures now cover idle (native 62.1 MiB vs Electron 197.6, fraction 0.314), streaming (91.9 vs 307.5, fraction 0.299), fresh-broker (Electron 202.1), and three Electron windows (1119 MiB, ≈460 MiB per added window — the strongest migration motivation measured). As of 2026-07-22 the preview has working selection/copy, Command-F search, a live accessibility value, verified light/dark switching, and passed a five-leg Developer ID packaged-swap continuity gate with real looping Claude and Codex CLIs streaming (all PIDs/epochs/offsets held; both numbered ladders complete). Local Developer ID signing works headless; the gate exposed and fixed silently-dropped Sparkle Info.plist keys and injected get-task-allow. The preview is installed at ~/Applications and shows the correct offline coexistence posture against the still-running 0.1.60 daily-driver broker. v0.1.93 (2026-07-22) closed the distribution gates: CI builds a parallel notarized native job (Sparkle helpers re-signed; embedded-framework preflight catches signing failures before the Apple round trip; dry-runnable via workflow_dispatch without burning a tag), the published zip verified staple/Gatekeeper/translocation, the rolling native-preview release hosts the locally-signed appcast, and a real Sparkle update took the installed preview 0.1.88→0.1.93 end to end with integrity intact. Still open: the installed-app Electron footprint baseline (awaiting natural daily-driver broker turnover), VoiceOver/Full-Keyboard-Access judgment rows, and a clean-user-account login test. Phase 1 is otherwise complete. **Phase 2 began 2026-07-22: the native app now owns terminals.** A sealed six-method controller lane on its own broker connection (create/attach/write/resize/kill/detachOwner) lets the app create folder-scoped shells via New Terminal (⌘T); their PTYs live on the broker so they survive app quit/update/crash (quit detaches, relaunch re-owns by project capability). The observer lane and its cannot-represent-mutation guarantee are untouched, so Electron-observed terminals stay strictly read-only. The full create→type→echo→quit(detach)→relaunch(reattach, same PID, continuous scrollback)→end loop was verified live on the real broker (`scripts/native-ownership-loop-probe.cjs`) and demonstrated in the running app: a native-owned shell ran `echo`/`uname` from real keyboard input with an "Interactive" badge, beside a read-only observed Electron terminal. Coexistence is covered by six Node tests (cross-ownership write rejection, explicit takeover, detach durability, kill isolation, observer read-only). **Phase 2 agent workspace (2026-07-22):** the native app is now a multi-session agent workspace. A New menu (sidebar + File menu bar) launches one-click Claude/Codex/OpenCode/Gemini agents as owned terminals booting the CLI; the sidebar shows each agent's live working/idle/responded state from the broker's `terminal:observer-activity` signal (immediate for the subscribed session, refreshed on a 2.5s timer for the rest) plus a seventh sealed controller method (`terminal.agentTurn`) so native-owned turns settle through the broker's 4.5s quiet timer exactly like Electron's. Sessions gained rename, agent-aware icons, and an Interactive/Working status line; the terminal palette now matches the Electron xterm theme. The agent-boot command path is proven (launches codex-cli via the exact login-shell form); Codex's `agentActivityProtocol.test.cjs` (6 tests) pins the broker contract. A full [migration roadmap](native-migration-roadmap.md) inventories every Electron feature with status + priority (P0/P1/P2) and a phase ordering. Still open in Phase 2: ACP chat surface (tool cards, thinking blocks, plans, permissions/gates, model/effort selection), multi-window + project tabs + the two nav layouts, settings/provider parity, and the packaged Developer ID control-plane gate. These commits are LOCAL (not pushed) per Michael's "massive iteration before next push" directive. **Phases A–D COMPLETE 2026-07-23 (pushed to origin/main, 163 native tests):** the "finish the preview" marathon closed every P0 and effectively every P1 agent-workspace row in the [roadmap](native-migration-roadmap.md). Shell: project tabs (open/rename/close/relocate/reorder/tints/activity badges), collapsible groups, ⌘K palette with fuzzy project-FILE search, workspace rail (⌘B) with file preview + markdown rendering + a plain editor (⌘S), Open Recent, saved window layouts, reopen closed project ⌘⇧T / session ⌘⌥T, full menu bar (Window/Help, Settings ⌘,), selection restore across launches. Chat depth: inline red/green diff artifacts, live agent-spawned ACP terminals (AcpTerminalHost answers terminal/create…release; the fs bridge is workspace-confined, symlink-resolved, sensitive-glob-guarded, and fs/write_text_file landed), permission rules + always-allow + sensitive globs (editable in Settings), permission-mode + effort/config-option pickers, slash-command autocomplete, queued follow-ups + Steer (interrupt+promote), optimistic-send retry, and pre-turn git checkpoints with confirmed restore. Terminal polish: font ⌘±, OSC 8 file links reveal in Finder, git-branch meta on rows, pop-out to window. Cross-cutting: a needs-you inbox (bell + dock badge) across projects, git panel with inline diffs/history/confirmed discard, CLAUDE_CONFIG_DIR/CODEX_HOME account isolation. **Kaisola Mesh v1 shipped:** one prompt fans out to every ACP agent, each column in an isolated kaisola-mesh-* git worktree with per-column diff review — proven end-to-end against real spawned adapters (distinct worktrees, both columns streamed full turns, cleanup removed worktrees AND branches). Named deferrals live in the roadmap (in-window split, pointer drag-reorder, staged Mesh pipeline, per-project account scoping, FS watch, syntax highlighting → WKWebView phase). **v0.1.99 (2026-07-23) — the parity mega-release: a 25-subagent, three-wave campaign closed every named deferral and the remaining P1 long tail.** Wave A+B (16 features): pointer drag-reorder of project tabs, session pinning, live OSC auto-titles, process/ports row meta, terminal font family/weight, staged Mesh (scout→execute) + idea mode + one-click Integrate, transcript paging + persisted chat drafts, browser cards on localhost links, OmniBar (⌘L), first-run onboarding, native notifications, toasts, usage center, API-key keychain, MCP settings registry (exact Electron wire shapes, carried into chats+Mesh), custom sequencing fixes. Wave C (8 features + solo items): prompt attachments (files/images as real ACP image/embedded-resource blocks — picker, drag-drop, paste), native regex syntax highlighting with read/edit toggle, CSV/TSV/JSON/HTML previews (confined JS-off web view), live FSEvents file tree, one-click Commit→Push→Create-PR (gh with compare-URL fallback), per-project Quick Action buttons, custom terminal-agent registration, per-project CLAUDE_CONFIG_DIR/CODEX_HOME scoping (plain shells now carry account env too — sign-in lands in the right dir), device-code sign-in card, word-level diffs with unified↔split toggle, Shift+Enter newline in terminals, ⇧⌘O open-in-external-editor, AGENTS.md scaffold. 389 native tests. A t3code competitive review confirmed Kaisola leads on the moats (native perf, durable PTYs, Mesh, guardrails); its two good ideas (PR flow, quick actions) were absorbed into this release.**

**Strategy — hybrid strangler → fully native.** Never a big-bang rewrite. The Electron app stays the daily driver and keeps shipping until the native app is demonstrably better. A native macOS shell (AppKit/SwiftUI on a shared `KaisolaCore` package) grows by *strangling* — it drives the existing battle-tested backends first, then ports each to Swift one at a time. Editor/docs live in a WKWebView (reusing today's React) at first, native later. The one truly durable, socket-speaking backend today is the detached `session-broker` (owns the PTYs); the native app reuses it unchanged first, then replaces it with a Swift PTY daemon last.

**Headline invariant — durable terminal/agent CLI runs survive app update/restart.** An in-flight `claude`/`codex` run keeps running while you update Kaisola; the relaunched app reattaches with continuous scrollback. Bounded honestly: survives app quit/relaunch/update, but *not* broker crash / logout / reboot, and history is retained up to the ~16 MiB spool with an explicit truncation marker.

**Migration phases (each ships; Electron stays the daily driver until parity):** 0) extract `KaisolaCore`, re-base the iPhone app on it · 1) native SwiftTerm terminal spike (observe-only, no ownership theft) · 2) control-plane host + native ACP / windows · 3) editor/docs via WKWebView bridge · 4) daily-driver parity (auth, settings, provider, MCP, companion, updates) · 5) full-native ports + drop Electron.

**Decisions locked 2026-07-21:** **A** — standalone authenticated Node control-plane host for ACP + MCP + Companion, with the durable session broker remaining a separate process · **B** — notarized Developer ID / non-sandboxed direct distribution with deliberately secured per-user helpers · **C** — separate Electron/native stores with idempotent one-way import and an explicit write-owner at cutover · **D** — dual-broker drain, so existing Node-owned PTYs finish naturally while new sessions eventually move to the Swift daemon. The retired desktop Board is not a native parity target; its legacy Companion wire DTOs remain Codable for protocol/fixture compatibility but are never consumed by the native desktop UI.

\- ~~I would like a main homepage window where the first window offical tab could be something like a board where I can see everything that's working what needs me~~ — **shipped 2026-07-17, retired 2026-07-20**. The desktop Board was removed after real use showed that it duplicated the project/window workflow and made navigation less predictable. Cross-window activity remains available in the deliberately lighter iPhone Companion.

\- Build an iPhone-first Kaisola Companion: securely pair with the desktop, see every project's agents and terminal streams, handle needs-you work, and control existing sessions without exposing local IPC or secrets. [Design](superpowers/specs/2026-07-17-mobile-companion-design.md) · [Plan](superpowers/plans/2026-07-17-mobile-companion.md) · [Native app](../mobile/KaisolaCompanion/README.md) — **in progress**: the signed-in, encrypted LAN app is implemented and on TestFlight. July 19 hardening adds a signed direct-LAN QR endpoint with Bonjour fallback, same-account “Find my Mac” rendezvous, cursor replay for transient reconnects with a coherent cold-launch snapshot, restored terminal subscriptions, and live activity projection for both manually launched Codex and Claude terminal agents (including Codex's `node` launcher). July 20 adds the guarded-control alpha: explicit per-device desktop grants; Face ID/passcode re-entry; real agent send/steer/stop and exact permission responses; a SwiftTerm VT surface; one 30-second, connection-bound lease per terminal with stale-write rejection, bounded paste, Ctrl-C, background release, and desktop-geometry restoration; and compatibility polling for still-running older brokers. The latest July 20 reliability pass adds stable paired-Mac identity recovery through Bonjour, automatic foreground resume after a cold relaunch, explicit reload, receipt-aware terminal subscription retries, reply-time activity clocks, correct responding-versus-idle status, automatic transcript follow, and minimal window/project-grouped Home and Sessions views. The restart/update recovery pass now persists the desktop listener intent, launches it quietly at Mac login, republishes Bonjour after wake, retries both ends automatically, synthesizes safe cursor history from still-running older brokers, suppresses redundant unsubscribe errors, and grants guarded controls during a confirmed pairing. The follow-up connection repair corrects TCP backpressure so accepted large terminal snapshots no longer disconnect the phone, keeps Bonjour discovery from preempting an in-flight Noise resume, preserves the complete bounded CLI replay, and carries an exact last-finished clock separately from live response bytes. Completion notifications now name the exact project session and provider. The desktop now has two persistent, live-switching navigation modes: a clearer compact Left project→session tree with utilities at its foot, or a single-row Top bar where clicking the active project expands/collapses its session group; saved windows attach to the project + cluster, while Usage and Settings live in the account menu. The native View menu and Settings expose the same layout choice, and software updates live in Settings. The current unreleased pass adds automatic LAN-first multipath reachability: optional Tailscale/Headscale private routing plus Kaisola Link, a hibernating account-scoped relay that verifies one-use tickets and forwards only the existing end-to-end encrypted Noise frames; desktop and iPhone retain the same replay, grants, receipts, and terminal leases on every route. It also adds crash-safe cross-window project transfer, right-click-only project renaming, contextual Markdown controls, focus-aware file search, and a strict dark-mode invariant across desktop, iPhone, and terminal surfaces. Phase 2 remains open for the full real-device/provider/network-switch/relay-restart race matrix and desktop live-control indicator polish. [Phase 1 design](superpowers/specs/2026-07-18-mobile-phase1-and-account-design.md)

\- Links and file links in terminal agent CLIs and agent MCP output should open in the file preview/browser on click — **implemented 2026-07-22, ships with the next release**: OSC 8 hyperlinks from agent CLIs now route (https → browser or localhost browser card, file:// → file preview with line scroll); bare URLs and filesystem-looking tokens in assistant tool rows are clickable; inline-code file citations like `src/foo.ts:12` in agent markdown open the preview at the line. Terminal regex-detected paths and markdown links already worked and are unchanged.

\- ~~sometimes when I copy paste a bunch of text with spaces from some output it copies and pastes it into the Command CLI agent terminal on kaisola as a bunch of messages instead of a single message and then it ends up queing messages~~ — **fixed 2026-07-17** (bracketed-paste mode state was lost on terminal remount; spool now tracks and replays DEC modes — commits `1207c96`, `0ec43a6`, `cc90f09`).

\- ~~when switching between windows or resizing terminal/agent tabs, I would like for the terminal not get scrolled up, the cli agent should stay at the recent outputs~~ — **fixed 2026-07-17** (sticky follow-intent: only deliberate scrolls unpin; resizes/tab-switches/remounts keep the terminal pinned to live output — commit `ea35821`)
